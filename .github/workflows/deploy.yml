name: API 배포 워크플로우 (미사용)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: 배포 경로 안내
        run: |
          echo "이 워크플로우는 운영 배포에 사용하지 않습니다."
          echo "NearPrice API 운영 배포는 SSM + S3 + docker compose 경로를 사용합니다."
          echo "문서: infra/terraform/AWS_TERRAFORM_FULL_GUIDE.md"
